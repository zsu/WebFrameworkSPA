(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.RoleUserListApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("roleUserListApi", ["$http", "$q", "$angularCacheFactory", $app.RoleUserListApi]);
})();
