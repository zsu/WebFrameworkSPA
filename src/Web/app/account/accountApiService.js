(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.AccountApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("accountApi", ["$http", "$q", "$angularCacheFactory", $app.AccountApi]);
})();
