(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.RolePermissionApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("rolepermissionApi", ["$http", "$q", "$angularCacheFactory", $app.RolePermissionApi]);
})();
