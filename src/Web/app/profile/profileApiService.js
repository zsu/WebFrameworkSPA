(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.ProfileApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("profileApi", ["$http", "$q", "$angularCacheFactory", $app.ProfileApi]);
})();
