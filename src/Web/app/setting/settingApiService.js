(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.SettingApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("settingApi", ["$http", "$q", "$angularCacheFactory", $app.SettingApi]);
})();
