(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.UserApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("userApi", ["$http", "$q", "$angularCacheFactory", $app.UserApi]);
})();
