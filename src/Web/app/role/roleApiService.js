(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.RoleApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("roleApi", ["$http", "$q", "$angularCacheFactory", $app.RoleApi]);
})();
