(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.LogsApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("logsApi", ["$http", "$q", "$angularCacheFactory", $app.LogsApi]);
})();
