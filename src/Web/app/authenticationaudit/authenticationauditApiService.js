(function () {
    /**
     * @param $http
     * @param $q
     * @param $angularCacheFactory
     */
    $app.AuthenticationAuditApi = function ($http, $q, $angularCacheFactory) {
    };

    app.service("authenticationAuditApi", ["$http", "$q", "$angularCacheFactory", $app.AuthenticationAuditApi]);
})();
